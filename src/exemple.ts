//version jeremy
//voir fight-club pour recup la methode guet user
//Comparaison d' email deja créer pour l'exercice
/*
bcrypt.compare(users.passworld, users.hashpass, (err, res) => {
  console.log("err", err);
  console.log("result", result);
  if (result) {
    console.log("Conexion reussi");
  } else {
    console.log("Echec de Connexion");
  }
});
// Utiliser jwt.io pour tester le token et les console.log
// La privateKey sera stocké de le .env.local
jwt.sign(
    { foo: "coucou" }, privateKey, { algorithm: 'RS256' } => (err, newtoken){
    console.log(newtoken);
});

/*
**/
